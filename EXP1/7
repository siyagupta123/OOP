#include<iostream>
using namespace std;
int main()
{
int n,temp,r,sum=0;
cout<<"Enter a number: ";
cin>>n;
temp=n;
while(n>0)
{
r=n%10;
sum=sum+(r*r*r);
n=n/10;
}
if(sum==temp)
cout<<"Armstrong number";
else
cout<<"Not an Armstrong number";
return 0;
}
